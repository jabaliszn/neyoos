# J.20 Audit — Future-Proof Configuration & Versioning
_Date: 2026-06-29_

## Verdict
**J.20 is NOT honestly complete.**

This section is currently marked **`(COMPLETED)`**, but a real repo audit shows it is only **partially built**.

The section header is also corrupted and duplicated in the checklist:
- `## J.20 — Future-Proof Configuration ## J.20 — Future-Proof Configuration & Versioning Versioning (COMPLETED)`

That should be cleaned during repair or downgrade.

---

## Honest line-by-line audit

### 1) Curriculum configurations must be versioned: e.g. CBC 2026, CBC 2027 update.
**Verdict: `[x]` Real**

What is real:
- `Curriculum` has real versioning-related fields:
  - `activeVersion`
  - `effectiveFrom`
  - `effectiveTo`
  - `status`
  - `previousVersionId`
- There is a real versioning service:
  - `src/lib/services/curriculum-versioning.service.ts`
- There is a real API route:
  - `src/app/api/curriculum/versions/route.ts`
- There is a real manager UI:
  - `src/components/academics/curriculum-version-manager.tsx`

Repo evidence:
- `prisma/schema.prisma`
- `src/lib/services/curriculum-versioning.service.ts`
- `src/app/api/curriculum/versions/route.ts`
- `src/components/academics/curriculum-version-manager.tsx`

This line is honestly `[x]`.

---

### 2) Schools can preview a future curriculum before switching.
**Verdict: `[x]` Real**

What is real:
- Draft versions can be created.
- Diff preview exists through `previewCurriculumDiff(...)`.
- UI includes a real `Preview Diff` action and dialog.

Repo evidence:
- `src/lib/services/curriculum-versioning.service.ts`
- `src/app/api/curriculum/versions/route.ts`
- `src/components/academics/curriculum-version-manager.tsx`

This line is honestly `[x]`.

---

### 3) Migration assistant shows what will change before applying updates.
**Verdict: `[~]` Partial**

What is real:
- J.2 has a real curriculum migration assistant for mapping legacy rows.
- J.20 has a real version diff preview between curriculum versions.

Why this is not fully honest yet:
- The **migration assistant itself** is not the thing showing the before/after update impact.
- The current diff is a version diff over learning areas, not a robust “migration assistant impact preview” across all affected school data.
- So the repo has **pieces**, but this line is still over-claimed.

Repo evidence:
- `src/lib/services/curriculum.service.ts` (migration assistant)
- `src/lib/services/curriculum-versioning.service.ts` (diff preview)

This should be `[~]`, not `[x]`.

---

### 4) Historical reports keep the curriculum version used at the time.
**Verdict: `[ ]` Not honestly built**

Why:
- I did not find a real report snapshot/version-stamp mechanism tying generated historical reports to a curriculum version.
- I did not find stored curriculum-version references on report outputs or report records.
- The UI/service copy says this is the goal, but the real persistence/proof is not there.

Repo evidence checked:
- `src/lib/services/report-template.service.ts`
- `src/app/api/academics/report-templates/route.ts`
- `prisma/schema.prisma`

This line should be `[ ]`.

---

### 5) Assessment and report templates versioned with effective dates.
**Verdict: `[ ]` Not honestly built**

Why:
- `AssessmentType` does **not** have effective date fields.
- `ReportTemplate` does **not** have effective date fields.
- I found no full-stack template versioning workflow for these items.
- The old audit/plan doc even described this as a future solution, which confirms it was not truly complete.

Repo evidence:
- `prisma/schema.prisma`
  - `AssessmentType` has no `effectiveFrom` / `effectiveTo`
  - `ReportTemplate` has no `effectiveFrom` / `effectiveTo`
- `src/lib/services/report-template.service.ts`

This line should be `[ ]`.

---

### 6) NEYO Ops can publish official curriculum templates for schools to adopt.
**Verdict: `[ ]` Already open, correctly**

This is already marked open in the checklist and belongs properly to J.21.

---

## Honest summary table

| J.20 line | Honest status | Reason |
|---|---|---|
| Curriculum configurations are versioned | `[x]` | Real DB + service + API + UI exist |
| Schools can preview future curriculum before switching | `[x]` | Real draft + diff preview flow exists |
| Migration assistant shows what will change before applying updates | `[~]` | Partial only; diff preview exists, but not a full migration-impact preview |
| Historical reports keep the curriculum version used at the time | `[ ]` | No real version-stamped historical-report persistence found |
| Assessment and report templates versioned with effective dates | `[ ]` | Schema/services do not actually implement this |
| NEYO Ops can publish official curriculum templates for schools to adopt | `[ ]` | Still open, correctly |

---

## Required checklist correction

### Current broken header
`## J.20 — Future-Proof Configuration ## J.20 — Future-Proof Configuration & Versioning Versioning (COMPLETED)`

### Honest header should become
`## J.20 — Future-Proof Configuration & Versioning (PARTIAL — audited 2026-06-29)`

---

## Bottom line
J.20 has a **real curriculum versioning foundation**.

But it is **not honest full-stack complete** because:
- migration-impact preview is only partial
- historical-report curriculum version locking is not really built
- assessment/report-template effective-date versioning is not really built

---

## Founder options

### Option 1 — Fix J.20 fully
We downgrade honestly first, then repair:
- header corruption
- migration-impact preview truthfully
- historical report version locking
- assessment/report-template effective-date versioning
- real J.20 passing test

### Option 2 — Downgrade honestly and move to J.21
We correct the checklist and continue the audit sequence.

### Option 3 — Your call
If you want, we can fix only the highest-value J.20 gaps first.
