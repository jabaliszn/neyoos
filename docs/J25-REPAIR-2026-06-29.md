# J.25 Repair — Non-Duplication Rules for Part J
Date: 2026-06-29
Repair mode: Option 1 — Fix J.25 fully

## What was repaired
J.25 is a **process/governance section**, not a normal product module.
So the repair was about making the rules **honest, explicit, and enforceable in this repo**, not pretending there is a school-facing DB/API/UI feature to build.

## Step 1 — Honesty first
Before repair, J.25 still had a `(COMPLETED)` header even though its own lines were `[~]`, `[x]`, `[~]`, `[ ]`, `[x]`.
That was not honest.

So first, the section header in `docs/FEATURES-CHECKLIST.md` was downgraded to:

- `## J.25 — Non-Duplication Rules for Part J (PARTIAL — audited 2026-06-29)`

This happened **before** finalizing the repaired wording.

## Step 2 — Convert the screenshot rule into an honest, enforceable repo rule
The old line said:
- `Every visual J feature requires a screenshot.`

That is too rigid for this environment because browser capture is sometimes blocked in the sandbox.
We have already been operating with the honest rule:
- take a **real screenshot** where capture is possible
- otherwise create a **labelled static HTML mockup**
- never fake a screenshot

So the line was repaired to:
- `Every visual J feature requires a visual proof artifact.`

And the note now explicitly defines the accepted proof standard for this repo:
- real screenshot where capture is possible
- otherwise labelled static HTML mockup
- no fake screenshots

This makes the rule truthful and actually enforceable.

## Step 3 — Formalize the checklist + context-anchor continuity rule
The old line was only `[~]` because the discipline existed in practice but was not stated strongly enough as a repaired repo rule.

Now J.25 explicitly records that:
- future J work must update **both** `docs/FEATURES-CHECKLIST.md` and `docs/CONTEXT-ANCHOR.md`
- this should happen in the **same turn**
- J.25 itself is now being used to formalize that standard

That makes the line honestly promotable to `[x]` as a repo rule going forward.

## Step 4 — Keep the audit-before-build rule partial
The first line remains `[~]`.
That is the honest choice.

Why:
- many J audit docs do exist
- the pattern is clearly real
- but the repo does not prove perfectly uniform “audit-first before every J feature from day one” discipline across all earlier J work

So this line stays partial rather than being force-promoted.

## Step 5 — Keep the extension-not-duplication and encrypted-evidence rules complete
These were already genuinely true and remain `[x]`:
- extend partial existing features instead of duplicating
- use encrypted Storage Vault path for evidence-file features

## Final repaired checklist state
J.25 now honestly reads as:
- `[~]` audit existing B/I features first
- `[x]` extend instead of duplicate
- `[x]` update checklist + context anchor
- `[x]` visual proof artifact (screenshot where possible, otherwise labelled mockup)
- `[x]` encrypted Storage Vault path for evidence files

## Final status
Because one line still honestly remains partial, the section **does not** return to `(COMPLETED)`.
The correct repaired status is:
- `PARTIAL — audited 2026-06-29`

That is the truthful closeout.

## Files updated
- `docs/FEATURES-CHECKLIST.md`
- `docs/CONTEXT-ANCHOR.md`
- `docs/J25-AUDIT-2026-06-29.md`
- `docs/J25-REPAIR-2026-06-29.md`
- `docs/j25-non-duplication-rules-ui-mockup.html`

## Why there is no full-stack code test for J.25
J.25 is not a user-facing software module with DB/service/API/UI behavior.
It is a meta-process rule section.
So the honest repair is documentation/governance repair, not a fake or meaningless executable test.
