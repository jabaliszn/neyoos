import { chromium } from 'playwright';
import { db } from '@/lib/db';
import { requestOnlineClass,setOnlineClassStatus } from '@/lib/services/online-class.service';
import type { SessionUser } from '@/lib/core/session'; import type { Role } from '@/lib/core/roles';
function asUser(u:any):SessionUser{return{id:u.id,tenantId:u.tenantId,neyoLoginId:u.neyoLoginId,fullName:u.fullName,phone:u.phone,email:u.email,role:u.role as Role,secondaryRole:u.secondaryRole as Role|null,language:u.language??'en'}}
async function main(){const teacher=asUser(await db.user.findFirstOrThrow({where:{email:'p.njoroge@karibuhigh.ac.ke'}})); const cls=await db.schoolClass.findFirstOrThrow({where:{tenantId:teacher.tenantId,archived:false}}); const s=await requestOnlineClass(teacher,{classId:cls.id,title:'debug',scheduledAt:'2099-09-01T08:00'}); await setOnlineClassStatus(teacher,s.id,'RUNNING'); const browser=await chromium.launch({headless:true,args:['--use-fake-ui-for-media-stream','--use-fake-device-for-media-stream']}); const context=await browser.newContext({viewport:{width:1440,height:900}, permissions:['camera','microphone']}); const page=await context.newPage(); page.on('console', m=>console.log('console',m.type(),m.text())); page.on('pageerror', e=>console.log('pageerror',e.message)); await page.goto('http://localhost:3000/login'); await page.evaluate(()=>localStorage.setItem('neyo-cookie-ack',new Date().toISOString())); await page.request.post('http://localhost:3000/api/auth/password/login',{data:{email:'p.njoroge@karibuhigh.ac.ke',password:'Karibu2026!'}}); await page.goto(`http://localhost:3000${s.joinUrl}`,{waitUntil:'domcontentloaded'}); await page.waitForTimeout(3000); console.log('before', (await page.textContent('body'))?.includes('Join mobile'), (await page.textContent('body'))?.includes('Leave')); await page.getByRole('button',{name:/Join mobile/i}).click({force:true}); await page.waitForTimeout(3000); const body=await page.textContent('body'); console.log('after contains', body?.includes('Leave'), body?.includes('Raise hand'), body?.includes('Mute all students'), body?.slice(0,500)); await page.screenshot({path:'screenshots/tmp-i90-debug.png'}); await browser.close(); await db.onlineClassSignal.deleteMany({where:{sessionId:s.id}}); await db.onlineClassParticipant.deleteMany({where:{sessionId:s.id}}); await db.onlineClassSession.deleteMany({where:{id:s.id}})}
main().finally(()=>db.$disconnect())
